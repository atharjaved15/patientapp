import 'package:flutter/material.dart';

class login_desktop extends StatelessWidget {
  const login_desktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Stack(
        children: [
          //Image.asset(name)
        ],
      ),
    );
  }
}
