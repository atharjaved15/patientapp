import 'package:flutter/material.dart';

class signup_desktop extends StatelessWidget {
  const signup_desktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
